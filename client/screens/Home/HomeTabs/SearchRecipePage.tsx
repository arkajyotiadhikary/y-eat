import { StyleSheet, Text, View } from "react-native";

const SearchRecipe = () => {
    return (
        <View>
            <Text>Search Recipe</Text>
        </View>
    );
};

export default SearchRecipe;
